/**
 * Trellis graph plugin host half; the interactive renderer ships in `./client`.
 * @module @deepseek-ai/dsh-client-ui-trellis-graph
 */

/** @returns nothing; this package contributes only a browser entry. */
export function apply(): void {}
