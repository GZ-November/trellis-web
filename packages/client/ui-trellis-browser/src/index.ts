/**
 * Trellis in-app browser plugin host half; the interactive view ships in `./client`.
 * @module @deepseek-ai/dsh-client-ui-trellis-browser
 */

/** @returns nothing; this package contributes only a browser entry. */
export function apply(): void {}
