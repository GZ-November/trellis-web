/**
 * Trellis workbench shell plugin host half; the full-screen knowledge surface
 * ships in `./client`.
 * @module @deepseek-ai/dsh-client-ui-trellis-shell
 */

/** @returns nothing; this package contributes only a browser entry. */
export function apply(): void {}
